#include <inttypes.h>

uint64_t read(void);
void write(uint64_t val);

int main() {
    for (int i=2; i<=9; ++i) {
        for (int j=2; j<=9; ++j) {
            int k = i * j;
        }
    }
    return 0;
}
